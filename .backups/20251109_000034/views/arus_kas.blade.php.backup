@extends('layout.main')
@section('title', 'Laporan Arus Kas')

@section('content')
<!-- <div class="card">
  <div class="card-header d-flex justify-content-between">
    <h3 class="card-title">Laporan Arus Kas ({{ strtoupper($mode) }})</h3>
    <div class="btn-group">
      <a href="{{ url('/jurnal/arus-kas/pdf?tanggal_awal='.$tanggalAwal.'&tanggal_akhir='.$tanggalAkhir.'&mode='.$mode) }}" class="btn btn-danger btn-sm">
        <i class="fas fa-file-pdf"></i> PDF
      </a>
      <a href="{{ url('/jurnal/arus-kas/excel?tanggal_awal='.$tanggalAwal.'&tanggal_akhir='.$tanggalAkhir.'&mode='.$mode) }}" class="btn btn-success btn-sm">
        <i class="fas fa-file-excel"></i> Excel
      </a>
    </div>
  </div>

  <div class="card-body"> -->
    <section class="content-header">
      <div class="card card-primary card-outline">
        <div class="card-header d-flex justify-content-between align-items-center">
          <h3 class="card-title font-weight-bold m-0">
            Laporan Arus Kas ({{ strtoupper($mode) }})
          </h3>
        </div>

        <div class="container col-md-10 pt-3">
          <form method="GET" action="{{ url('/jurnal/arus-kas') }}" class="mb-3">
            <div class="form-row align-items-end">
              <div class="col-md-2">
                <label>Tanggal Awal</label>
                <input type="date" name="tanggal_awal" value="{{ $tanggalAwal }}" class="form-control">
              </div>
              <div class="col-md-2">
                <label>Tanggal Akhir</label>
                <input type="date" name="tanggal_akhir" value="{{ $tanggalAkhir }}" class="form-control">
              </div>
              <div class="col-md-2">
                <label>Metode</label>
                <select name="mode" class="form-control">
                  <option value="direct" {{ $mode=='direct' ? 'selected' : '' }}>Langsung</option>
                  <option value="indirect" {{ $mode=='indirect' ? 'selected' : '' }}>Tidak Langsung</option>
                </select>
              </div>

              <!-- Tombol Filter -->
              <div class="col-md-2">
                <button type="submit" class="btn btn-primary">
                  <i class="fas fa-filter"></i> Filter
                </button>
              </div>

              <!-- Tombol Export -->
              <div class="col-md-3">
                <div class="form-group mb-0 text-md-right mt-3 mt-md-0">
                  <a href="{{ url('/jurnal/arus-kas/pdf?tanggal_awal='.$tanggalAwal.'&tanggal_akhir='.$tanggalAkhir.'&mode='.$mode) }}"
                  class="btn btn-danger btn-sm">
                  <i class="fas fa-file-pdf"></i> Export PDF
                </a>
                <a href="{{ url('/jurnal/arus-kas/excel?tanggal_awal='.$tanggalAwal.'&tanggal_akhir='.$tanggalAkhir.'&mode='.$mode) }}"
                class="btn btn-success btn-sm">
                <i class="fas fa-file-excel"></i> Export Excel
              </a>
            </div>
          </div>
        </div>
      </form>

      <div class="card-body">
        <p><strong>Saldo Awal Kas:</strong> Rp {{ number_format($saldoAwal, 2) }}</p>

        @php $arus = $mode == 'direct' ? $arusKasDirect : $arusKasIndirect; @endphp

        <table class="table table-bordered">
          <thead class="thead-dark">
            <tr><th>Kategori</th><th class="text-right">Nominal (Rp)</th></tr>
          </thead>
          <tbody>
            <tr><td>Arus Kas Operasional</td><td class="text-right">{{ number_format($arus['operasional'], 2) }}</td></tr>
            <tr><td>Arus Kas Investasi</td><td class="text-right">{{ number_format($arus['investasi'], 2) }}</td></tr>
            <tr><td>Arus Kas Pendanaan</td><td class="text-right">{{ number_format($arus['pendanaan'], 2) }}</td></tr>
          </tbody>
          <tfoot>
            <tr class="bg-secondary text-white">
              <th>Kenaikan/Penurunan Kas Bersih</th>
              <th class="text-right">{{ number_format(array_sum($arus), 2) }}</th>
            </tr>
            <tr class="bg-light">
              <th>Saldo Akhir Kas</th>
              <th class="text-right">{{ number_format($saldoAkhir, 2) }}</th>
            </tr>
          </tfoot>
        </table>
      </div>
    </div>
  </div>
</section>

@endsection
